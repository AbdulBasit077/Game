import 'package:flutter/material.dart';

class AppBorderRadius{
  static BorderRadius  borderRadius05=const BorderRadius.all( Radius.circular(05.0) );
  static BorderRadius  borderRadius10=const BorderRadius.all( Radius.circular(10.0) );
  static BorderRadius  borderRadius16=const BorderRadius.all( Radius.circular(16.0) );
  static BorderRadius  borderRadius20=const BorderRadius.all( Radius.circular(20.0) );
  static BorderRadius  borderRadius25=const BorderRadius.all( Radius.circular(25.0) );
  static BorderRadius  borderRadius50=const BorderRadius.all( Radius.circular(50.0) );
  static BorderRadius  borderRadius100=const BorderRadius.all( Radius.circular(100.0) );

}